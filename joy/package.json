{
  "name": "joy-agent",
  "version": "1.0.0",
  "description": "Two-model orchestration agent: plan, build, verify, refine.",
  "bin": {
    "joy": "./bin/joy.js"
  },
  "scripts": {
    "start": "node ./bin/joy.js",
    "postinstall": "python3 -m pip install -r requirements.txt || pip install -r requirements.txt || true"
  },
  "keywords": ["claude", "anthropic", "agent", "orchestration"],
  "license": "MIT"
}
